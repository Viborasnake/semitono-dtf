import {garmentPresets} from './garment-presets.ts'

// Shared with the categorized picker so the recommended white setup and
// engine regression tests always use exactly the same processing values.
export const whiteGarmentHalftone = garmentPresets.lightGarment.values
